{
  "name": "poketokenbar-landing",
  "private": true,
  "scripts": {
    "preview": "python3 -m http.server 8765 --bind 127.0.0.1",
    "test": "node scripts/verify.cjs",
    "design:check": "sh scripts/check-design.sh",
    "design:install": "npx --yes impeccable@4.1.0 install --providers=codex --scope=project --yes"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "playwright": "1.62.1"
  }
}
